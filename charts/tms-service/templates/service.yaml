# SPDX-License-Identifier: Apache-2.0
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tms-service.fullname" . }}
  labels:
    {{- include "tms-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.sidecar.port }}
      targetPort: {{ .Values.sidecar.port }}
      protocol: TCP
      name: sidecar
  selector:
    {{- include "tms-service.selectorLabels" . | nindent 4 }}
