# SPDX-License-Identifier: Apache-2.0
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tms-service.fullname" . }}-test-connection"
  labels:
    {{- include "tms-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tms-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
