# SPDX-License-Identifier: Apache-2.0

# Default values for tms-service.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# This will set the replicaset count more information can be found here: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/
replicaCount: 1

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  repository: tazamaorg/tms-service
  # This sets the pull policy for images.
  pullPolicy: Always
  # The image tag is intentionally left blank.
  # It is dynamically set by the CI/CD pipeline during deployment
  # based on the version in package.json.
  tag: "4.0.0"

sidecar:
  name: tms-event-sidecar
  image:
    repository: tazamaorg/event-sidecar
    # The image tag is intentionally left blank.
    # It is dynamically set by the CI/CD pipeline during deployment
    # based on the version in package.json.
    tag: "4.0.0"
    pullPolicy: Always
  port: 5000

# This is for the secrets for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets:
  - name: frmpullsecret
# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

# This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: false
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations:
  prometheus.io/scrape: 'false'
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext:
  runAsNonRoot: true
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: ClusterIP
  # This sets the ports more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#field-spec-ports
  port: 3000

# This block is for setting up the ingress for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingress:
  enabled: true
  className: "nginx"
  annotations:
     # If cert-manager is installed, use the following annotation to automatically create a TLS secret
     cert-manager.io/cluster-issuer: letsencrypt-issuer
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: tms-service.sandbox.test.lf
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
    - secretName: tms-service-tls
      hosts:
        - tms-service.sandbox.test.lf

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

# This is to setup the liveness and readiness probes more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
livenessProbe:
  httpGet:
    path: /
    port: http
readinessProbe:
  httpGet:
    path: /
    port: http

# This section is for setting up autoscaling more information can be found here: https://kubernetes.io/docs/concepts/workloads/autoscaling/
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# Additional volumes on the output Deployment definition.
volumes: []
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

# Additional volumeMounts on the output Deployment definition.
volumeMounts: []
# - name: foo
#   mountPath: "/etc/foo"
#   readOnly: true

nodeSelector: {}

tolerations: []

affinity: {}

env:
  mode: "http"
  upstream_url: "http://0.0.0.0:3000"
  exec_timeout: "10s"
  write_timeout: "15s"
  read_timeout: "15s"
  prefix_logs: "false"
  FUNCTION_NAME: "transaction-monitoring-service"
  NODE_ENV: "production"
  PORT: 3000
  QUOTING: "false"
  SERVER_URL: ""
  MAX_CPU: "0.5"
  AUTHENTICATED: "false"
  CERT_PATH_PUBLIC: ""
  REDIS_DATABASE: 0
  REDIS_AUTH: "tazamaValkey"
  REDIS_SERVERS: "valkey.processor.svc.cluster.local"
  REDIS_IS_CLUSTER: ""
  DISTRIBUTED_CACHETTL: 300
  DISTRIBUTED_CACHE_ENABLED: "true"
  LOCAL_CACHETTL: 300
  LOCAL_CACHE_ENABLED: "true"
  NATS_SERVER_URL: "nats://nats:4222"
  STARTUP_TYPE: "nats"
  PRODUCER_STREAM: ""
  ACK_POLICY: "Explicit"
  PRODUCER_STORAGE: "File"
  PRODUCER_RETENTION_POLICY: "Workqueue"
  RAW_HISTORY_DATABASE: "raw_history"
  RAW_HISTORY_DATABASE_HOST: "postgresql.processor.svc.cluster.local"
  RAW_HISTORY_DATABASE_PORT: "5432"
  RAW_HISTORY_DATABASE_USER: "tazama"
  RAW_HISTORY_DATABASE_PASSWORD: "tazamaPassword"
  RAW_HISTORY_DATABASE_CERT_PATH: "/usr/local/share/ca-certificates/ca-certificates.crt"
  EVENT_HISTORY_DATABASE: "event_history"
  EVENT_HISTORY_DATABASE_HOST: "postgresql.processor.svc.cluster.local"
  EVENT_HISTORY_DATABASE_PORT: "5432"
  EVENT_HISTORY_DATABASE_USER: "tazama"
  EVENT_HISTORY_DATABASE_PASSWORD: "tazamaPassword"
  EVENT_HISTORY_DATABASE_CERT_PATH: "/usr/local/share/ca-certificates/ca-certificates.crt"
  APM_ACTIVE: "false"
#    APM_SERVICE_NAME: "transaction-monitoring-service"
#    APM_URL: "http://apm-server.development.svc.cluster.local:8200/"
#    APM_SECRET_TOKEN: ""
  LOG_LEVEL: "info"
  SIDECAR_HOST: "0.0.0.0:5000"
  CORS_POLICY: "prod"


# Controls whether the tms-service chart should be installed.
installTmsService: true